# Only examples/jev.mjs reads these variables. Never commit your real key.
TYPESAFE_API_KEY=
# Explicitly set a tested version before production use. Alias is for experiments.
TYPESAFE_MODEL=jev-latest
